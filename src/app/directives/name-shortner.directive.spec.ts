import { NameShortnerDirective } from './name-shortner.directive';

describe('NameShortnerDirective', () => {
  it('should create an instance', () => {
    const directive = new NameShortnerDirective();
    expect(directive).toBeTruthy();
  });
});
